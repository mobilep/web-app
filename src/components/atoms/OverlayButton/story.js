import React from 'react';
import { storiesOf } from '@storybook/react';

import OverlayButton from './';

storiesOf('1. Atoms / OverlayButton', module)
	.add('OverlayButton', () => <OverlayButton />);
