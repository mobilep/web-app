import React from 'react';
import { storiesOf } from '@storybook/react';
import VideoRecorder from './VideoRecorder';

storiesOf('2. Molecules / VideoRecorder', module)
	.add('VideoRecorder', () => <VideoRecorder />);
