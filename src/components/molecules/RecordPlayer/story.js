import React from 'react';
import { storiesOf } from '@storybook/react';
import RecordPlayer from './RecordPlayer';

storiesOf('2. Molecules / RecordPlayer', module)
	.add('RecordPlayer', () => (<RecordPlayer />));
