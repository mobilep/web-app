import React from 'react';
import { storiesOf } from '@storybook/react';
import ScenarioMobileHeader from './ScenarioMobileHeader';

storiesOf('2. Molecules / ScenarioMobileHeader', module)
	.add('default', () => (<ScenarioMobileHeader />));
